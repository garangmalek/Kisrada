// ─── App-wide constants ───────────────────────────────────────────────────────
// Keeping all magic numbers/strings here makes them easy to update in one place.

export const API_URL = 'https://forkify-api.jonas.io/api/v2/recipes/';

// Seconds before a fetch is considered timed out
export const TIMEOUT_SEC = 10;

// How many search results to show per page
export const RES_PER_PAGE = 10;

// Required only for uploading/adding your own recipes
export const KEY = '841445ca-79f0-4055-9957-f7cd8eda3bb9';

// Seconds the "success" message shows before the modal closes
export const MODAL_CLOSE_SEC = 2.5;
